{
  "name": "rotbarsch-reqnroll-vscode-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI tests for the Rotbarsch Reqnroll VS Code extension",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
